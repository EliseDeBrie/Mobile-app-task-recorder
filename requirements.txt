opencv-python
python-docx
numpy
pynput
mss
# Optional: text-pattern redaction (also needs a Tesseract install)
# pytesseract
# Optional: pick the app by window title instead of dragging a region
# pygetwindow
